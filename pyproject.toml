[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "TRMbeta"
version = "0.1"
dependencies = ["numpy",
"scipy",
"twine"]
requires-python = ">=3.7"
authors = [
  {name = "Diogo L Pires", email = "diogo.l.pires@gmail.com"},
]
readme = "README.md"